# The application assumes that you are running it on redis server having version greater than 6.2, refer https://redis.io/download to download latest version.

# To test it please follow the following steps

1. install the relevant packages 'npm i',
2. run script 'node scripts/populate_sample_data.js',
3. node index.js
4. open the api collection (API Collection Link - https://www.getpostman.com/collections/29b96def28adf09898f3), and test individual APIs.
